# Stripe Configuration (REQUIRED)
# Get your test keys from: https://dashboard.stripe.com/test/apikeys
STRIPE_API_KEY=sk_test_51NNsuf.....

# Stripe Webhook Secret (REQUIRED for webhook verification)
# For local development with Stripe CLI:
#   1. Run: stripe listen --forward-to localhost:8000/api/v1/stripe/webhook
#   2. Copy the webhook signing secret (whsec_...) from the output
# For production:
#   1. Create webhook endpoint in Stripe Dashboard
#   2. Copy the signing secret from the webhook details
STRIPE_WEBHOOK_SECRET=whsec_6677....

